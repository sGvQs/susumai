{
  "name": "susumai",
  "version": "0.1.0",
  "description": "Zero-dependency CLI to chat with a self-hosted Ollama (DeepSeek R1) over a Cloudflare tunnel.",
  "license": "MIT",
  "author": "susum",
  "type": "module",
  "bin": {
    "susumai": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "repository": "github:susum/susumai",
  "scripts": {
    "build": "tsup",
    "test": "node --test",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
